$(function(){
  
  $('.top-slider__inner').slick({
    dots: true,
    arrows: false,
    autoplay: true,
    autoplaySpeed: 4000,
  });

  var mixer = mixitup('.products__items');
  var mixer__design = mixitup('.design__inner');
  
});